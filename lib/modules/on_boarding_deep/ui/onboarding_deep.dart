import 'package:flutter/material.dart';

class OnboardingDeep extends StatelessWidget {
  const OnboardingDeep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}